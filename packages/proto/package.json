{
  "name": "@pf/proto",
  "version": "0.1.0",
  "private": false,
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src", "proto", "gen"],
  "scripts": {
    "generate": "buf generate",
    "build": "pnpm run generate && tsc -p tsconfig.json",
    "clean": "rimraf dist gen || true",
    "lint": "echo \"(todo)\"",
    "test": "echo \"(todo)\""
  },
    "dependencies": {
    "@bufbuild/protobuf": "^2.2.0",
    "@grpc/grpc-js": "^1.11.0",
    "long": "^5.2.3"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "@pf/tsconfig": "workspace:*"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  }
}
